export type Recipe = {
    id: string,
    name: string,
    ingredients: string[],
    instructions: string[],
    cookingTime: string,
    servings: number,
    image: string,
    description: string,
    background: string,
};